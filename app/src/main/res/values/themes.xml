<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MoodJournal" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MoodJournal" parent="Base.Theme.MoodJournal" />

    <style name="Theme.MoodJournal.Splash" parent="Theme.SplashScreen">
        <!-- Background -->
        <item name="windowSplashScreenBackground">@color/good</item>

        <!-- App icon -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>


        <item name="postSplashScreenTheme">@style/Theme.MoodJournal</item>
    </style>

</resources>